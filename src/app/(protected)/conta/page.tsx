import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Conta",
  description: "Conta",
};

export default function AccountPage() {
  return (
    <section>
      <h1>Conta</h1>
    </section>
  );
}
