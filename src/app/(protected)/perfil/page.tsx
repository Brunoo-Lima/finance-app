import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Perfil",
  description: "Perfil",
};

export default function ProfilePage() {
  return (
    <section>
      <h1>Perfil</h1>
    </section>
  );
}
